# Project knowledge (animus)

Durable lessons for future agents. Not a backlog or duplicate of `project_history.md`.

---

## mitmproxy + ANIMUS / Hermes gateway (010526)

- **Stable default:** set mitm **`HTTP_PROXY` / `HTTPS_PROXY` / `NO_PROXY=127.0.0.1,localhost,::1`** on **`~/.config/systemd/user/hermes-gateway.service`** only. **Do not** set those on repo-root **`animus.env`** — ANIMUS→**`127.0.0.1:8642`** over httpx saw flaky failures / races with proxy enabled. Provider **`model` + `messages`** still appear in mitm on **gateway→provider HTTPS**. After unit edits: **`systemctl --user daemon-reload`** + **`restart hermes-gateway.service`** (and **`animus.service`** if **`animus.env`** changed).
- **Legacy “both files” pairing** is deprecated for this host after 010526; examples and **`repo_map.md`** reflect gateway-only proxy.
- **Cursor `http.proxy`** does not apply to **`animus.service`** (Python **httpx**); gateway Python honors the same env vars when set on the unit.
- **`NO_PROXY=127.0.0.1,localhost,::1`:** keeps **local Hermes HTTP** off mitm (avoids **502 / connection errors**). For **“which model / full prompt sent out”**, inspect mitm flows for **gateway → provider HTTPS** (e.g. **`api.openai.com`**, Codex, Anthropic): the **request JSON** carries **`model`** and **`messages`** as sent upstream — that is the authoritative capture vs browser UI labels. **ANIMUS → `127.0.0.1:8642`** is intentionally not in that capture path.
- **When the mitm block is enabled**, **mitmproxy must listen on `127.0.0.1:8080`** or outbound calls fail. If provider TLS fails, trust mitm CA system-wide or set **`SSL_CERT_FILE`** for the gateway process. Templates: **`animus.env.example`**, **`animus-chat/animus.env.example`**.
- **Reliability toggle (010526 live check):** if chats start failing with `API call failed after 3 retries: Connection error`, first disable gateway proxy env lines in `~/.config/systemd/user/hermes-gateway.service` and restart gateway/chat. On this host, Codex immediately recovered (`CODX_OK`) when those proxy lines were commented.
- **Forensic provider-proof recipe (010526):** use a **separate temporary mitm port** (e.g., `8083`) instead of the shared `8080` session to avoid mixed noise. Point only `hermes-gateway.service` to that port (`HTTP_PROXY`/`HTTPS_PROXY` + loopback `NO_PROXY`), add `SSL_CERT_FILE=/home/sketch/.mitmproxy/mitmproxy-ca-cert.pem` for TLS trust, restart gateway, run one provider-forced ANIMUS `/api/chat` request per capture window, and inspect the mitm JSONL for upstream host/path/model (Codex→`chatgpt.com/backend-api/codex/responses`, Claude→`api.anthropic.com/v1/messages`, Cursor→`api2.cursor.sh/*`). After capture, **revert unit env lines and restart gateway**.

## Profile lock hardening (010526)

- **Cursor browser-auth fallback (010526):** if `cursor-agent` fails with an invalid `CURSOR_API_KEY` message, retry once with that env var removed so CLI login (`cursor login`) can be used. This prevents stale env keys in `~/.hermes/.env` from breaking browser-auth Cursor runs.
- **Per-request provider override in API server:** when `hermes_provider` or `hermes_base_url` is overridden, recompute `api_mode` from provider/base URL (`determine_api_mode`). Otherwise stale default-mode values (e.g. `codex_responses`) can misroute Cursor/Copilot shims and trigger `.responses` attribute errors.
- **Per-request provider override must re-resolve full runtime config (010526):** recomputing `api_mode` alone is insufficient for alias providers such as `claude-code`. If `provider` changes but stale `base_url`/`api_key` from the default runtime remain, requests can silently hit the wrong upstream (e.g. Codex endpoint) and hang/fail. In `gateway/platforms/api_server.py`, use `resolve_runtime_provider(requested=..., explicit_base_url=..., target_model=...)` and overwrite `provider/base_url/api_mode/api_key/command/args/credential_pool` together.
- **Hermes default profile path is `~/.hermes`** (not `~/.hermes/profiles/default`). If ANIMUS uses the latter while gateway runs with `--profile default`, `/api/hermes-chat-meta` shows mismatch and Settings/model sync can target the wrong profile.
- **Pin gateway profile in systemd ExecStart:** use `... -m hermes_cli.main --profile default gateway run --replace` in `hermes-gateway.service`. This prevents `~/.hermes/active_profile` (e.g., `dpc` symlinked to companion home) from silently hijacking ANIMUS traffic.
- **Restart hardening in `animus-chat/server.py`:** `POST /api/restart/gateway` now prefers systemd restart first; if fallback CLI is needed it passes `--profile <derived-from-HERMES_HOME>`. This avoids profile drift during restart paths.
- **`scripts/sync-dev-systemd.sh` must keep `HERMES_HOME=$HOME/.hermes`** and normalize legacy `HERMES_HOME=.../.hermes/profiles/default` in existing `animus.env` files.

## Release zip size (010526)

- **`hermes-agent/.e2e-venv/`** is gitignored but still lives on disk; **`build-release.sh`** must **`zip -x`** it or the buyer zip balloons past the **55MB** cap (symptom: ~77MB). **`artifacts/`** is gitignored and excluded from zip so internal QA markdown does not ship to buyers.
- **`version archive/`** (or any folder of local **`animus-v*.zip`** copies under the repo root) is **not** gitignored; if present, **`build-release.sh`** must **`zip -x "./version archive/*"`** or the ship zip can exceed **~290MB** (nested old zips). The script also fails the zip leak check if **`version archive/`** appears inside the archive.

## Tokens tab (300426)

- **`animus-chat/token_usage.py`:** **`read_usage_all(source)`** reads the whole **`token_usage.jsonl`** (no date cutoff). **`GET /api/tokens/usage?full=1`** (also **`full=true`**) returns all rows + summary; optional Hermes analytics uses **366** days when dashboard token is set.
- **`animus-chat/app/index.html`:** Local rows come from **`collectTokenUsageEntriesAll()`** (all **`msg.usage`** in loaded convs). Server rows from **`mergeServerTokenEntriesFull`**: **`fetch('/api/tokens/usage?full=1&source=all')`**. Same **chat dedupe** as before: if any server row has **`source === 'chat'`**, merge **`server.concat(local without chat)`** so **`msg.usage`** is not double-counted with JSONL.
- **Charts:** **This month (by day)** — same color cycle; bar height uses **sqrt** with scale ceiling **`TOKEN_DAILY_CHART_SCALE_CAP` = 3_000_000** tokens (via **`renderSolidTokenBarChart(..., { sqrtScaleCapTokens })`**) so one huge day does not flatten the rest. **All-time by calendar month** — **`aggregateAllCalendarMonthsSeries`** (one bar per local month, chronological). **By year** — **`aggregateYearTotalsSeries`**. Header summary: **this month + all-time** totals only (legend line removed).
- **Breakdown tables:** **`collectTokenUsageBreakdownByPeriods`** + **`tokenUsageBreakdownPeriodBounds(nowMs)`** — per label, totals for **today**, **last 7 local days** (inclusive), **current local calendar month**, **current local calendar year**, plus sort key **`all`**. Pass the same **`nowMs`** as the Tokens tab render snapshot. **`renderTokenUsageBreakdown`** renders a grid (Day / Week / Month / Year). Hint: **`#tokenUsageBreakdownPeriodHint`**.
- **Tokens local time:** **`formatTokenUsageLocal12h(ms)`** (`toLocaleString`, `hour12: true`, `timeZoneName: 'short'`) for server log rows and CSV **`ts_local_12h`**. **`#tokenUsageTimeContext`** shows **`Intl.DateTimeFormat().resolvedOptions().timeZone`**, GMT offset for refresh instant, and **Refreshed …** in 12h local. Records CSV: comment lines **`# refreshed_at_local`**, **`# time_zone`**, **`date_local`** via **`formatTokenUsageLocalDateYMD`** (local calendar date). Export filename uses **`toLocaleDateString('en-CA')`** from export time.
- **Recent server log (0-token UI):** Wrapped in **`<details class="token-server-log-details">`**, **collapsed by default**. **`sessionStorage`** keys **`animus_token_server_log_open_day`** (local **`en-CA`** date) + **`animus_token_server_log_open`** restore expanded only when the day matches — new calendar day starts collapsed again. List stays capped at **20** rows in the UI; full sorted tail is **`serverRecentFull`** for the CSV snapshot only.
- **Tokens `animus_client` + slugs:** **`token_usage.ANIMUS_CLIENT_SLUGS`** allowlist (**`web`**, **`chat`**, **`plan`**, **`skills`**, **`wizard`**, **`help`**, **`cron`**, **`prompt-optimizer`**). PWA sets **`X-Animus-Client`** via **`animusClientFetchHeaders(base, slug)`** (**`chat`** main send, **`plan`** Plan **`/api/chat`**, **`/api/tokens/record`** uses **`animusClientSlugFromTokenMeta`**, **`help`** / **`prompt-optimizer`** on those fetches). Server stamps **`animus_client`** on **`record_token_usage`** for **help**, **cron** trigger rows, **cron** **`optimize-prompt`** completions (**`chat_usage_in_out`**). **`_infer_proxy_chat_provider_slug`** on **`/api/chat`** only when **`animus_client_from_request`** is **None**.
- **Regression proof (animus-chat):** `cd animus-chat && .venv/bin/python -m unittest tests.test_token_usage_animus_client -v` — **14** tests: allowlist normalization, **`POST /api/tokens/record`** headers (**`chat`/`plan`/`skills`/`wizard`/`web`**), negative **`X-Animus-Client: random-test`** (no **`animus_client`** field), mocked Help + cron paths (**`help`**, **`cron`**, **`prompt-optimizer`**), **`GET /api/tokens/usage?full=1`** echoes **`animus_client`**, static **`app/index.html`**: CSV **`animus_client`** column, **`ANIMUS:`** recent-log tag, **`#tokenUsageAnimusClientList`** + **`aggregateAnimusClientTotals`** / **`renderAnimusClientBreakdown`** + **Uncategorized / missing**, **By source (logging)** label. Requires **`docs/animus-user-guide.md`** for Help tests. **/api/chat** SSE not in this suite. Operator narrative: **`artifacts/token_tracker_tab_explained.md`**.
- **Tokens tab two axes:** JSONL **`source`** (chat, help, cron, plan, …) powers period breakdown **tabs**; **`animus_client`** (product surface) powers the **ANIMUS client** table + CSV + recent-log **`ANIMUS:`** tag. Server **`GET /api/tokens/usage` `summary`** has no **`by_animus_client`** — client breakdown is computed in **`index.html`** from merged rows.
- **Browser E2E vs status:** Backend + static UI = **PASS**; live Tokens tab + downloaded CSV bytes = **PARTIAL** until an operator runs **`artifacts/token_tracker_tab_explained.md`** §10 checklist and records evidence.
- **Tokens tab auto-refresh:** While **Tokens** is the active sidebar tab, **`renderTokenUsageModal`** runs on a **10-minute** interval (`TOKEN_USAGE_AUTO_REFRESH_MS`); timer cleared when leaving the tab; ticks skipped when **`document.visibilityState`** is not **`visible`**.
- **Token charts collapsible:** Three **`<details class="token-usage-chart-details">`** in **`#tab-tokens`**: **This month (by day)** (**`#tokenUsageChartDetailsThisMonth`**) default **`open`**; **By calendar month (all-time)** and **By calendar year (all-time)** default **closed**. **`scheduleTokenUsageChartReflow`** (120ms debounce) re-runs **`renderTokenUsageModal`** when a chart section is expanded so **`clientHeight`** is valid for bar layout.
- **Status wording:** Treat as **PASS** for the **backend allowlisted `animus_client` contract** when the unittest file above is green. Say **PARTIAL** or **not E2E-verified** only if also claiming full **`/api/chat`** stream logging, CSV export in the browser, or Tokens tab “Recent server log” rendering — those paths are intentionally out of that suite.
- **Tokens layout CSS (server log + charts):** **`summary`** must not use **`list-style-position:outside`** (disclosure triangle draws outside the border). Use flex **`summary`**, hide native marker, **`::before` chevron**; wrap charts in **`.token-usage-chart-block`**; **`token-usage-chart--spread`** when **`series.length ≤ 20`**. If UI regress after merge, grep **`token-server-log-summary::before`** / **`token-usage-chart-block`** in **`index.html`**; bump **`sw.js`** **`CACHE`** + **`CHAT_SERVER_REV`** so the PWA refetches shell HTML.
- **Legacy layout gotcha (fixed):** **`.token-usage-chart`** needs **`align-items: stretch`** (or equivalent) so **`.token-bar-stack`** pixel heights resolve; bump **`sw.js`** + **`CHAT_SERVER_REV`** when changing token tab HTML/CSS.

## Hermes project session priming E2E (010526)

- **Artifact:** **`artifacts/hermes_project_session_priming_e2e.md`** — commands, **`tests/gateway/test_api_server.py`** (**125 passed** after session-prompt-status + regression tests), SSE **`hermes.session`**, preflight contract; **§2** now holds **2026-05-01** live paste (Playwright + curl) with **Overall live-stack status: PASS**. **Ops:** if streams lack `event: hermes.session` while repo code has it, restart **`hermes-gateway.service`** (stale process). **`GET /health/detailed`** → `hermes_home_resolved` is the source of truth for which **`state.db`** to query (ANIMUS meta may show a different profile path).
- **PEP 668:** On Debian/Ubuntu, `pip install aiohttp` against system `python3` fails; use a **throwaway venv** under **`hermes-agent/`** (e.g. **`.e2e-venv/`**, gitignored), `pip install -e .`, then `PYTEST_ADDOPTS='' python -m pytest tests/gateway/test_api_server.py` (project **`addopts`** may include **`-n`** → needs **pytest-xdist**).
- **Stale omit mitigation:** Before each project **`send()`** with an assistant message, **`reconcileHermesStoredPromptFromServer`** calls **`GET /api/hermes/session-prompt-status`** (gateway **`/v1/chat/session-prompt-status`**). **`buildMessages`** omits the project block only when **`hermes_has_stored_system_prompt === true`** and **`hermes_stored_prompt_confirmed_session_id`** equals the current **`session_id`**. Gateway **404** on preflight → leave local state unchanged (legacy gateway).

## Hermes chat session per ANIMUS conversation (300426)

- **SQLite system prompt reload (gateway):** In **`hermes-agent/run_agent.py`** `run_conversation`, the path that sets **`_cached_system_prompt`** from **`SessionDB.get_session(...).system_prompt`** must **not** require **`conversation_history`** to be non-empty. The HTTP gateway constructs a **new** `AIAgent` per message; gating on history caused every turn to fall through to **`_build_system_prompt()`** again (~multi‑k token core rebuild + unstable prefix cache) even when **`sessions.system_prompt`** already held the merged snapshot — symptom: similar **input** token totals on every turn instead of growing mainly with thread length. **Validation:** `PYTEST_ADDOPTS='' pytest tests/run_agent/test_run_agent.py::TestSystemPromptStability`.
- **Client:** **`ensureHermesSessionIds(convs)`** backfills **`session_id`** (UUID) for any conv missing it (legacy data, server import). Runs after localStorage load (persist + **`queueMicrotask(_scheduleServerSync)`** so sync helpers exist), after **`applyServerConversationsList`**, and before each **`send()`** via **`ensureHermesSessionIds([conv]); save()`**. Chat JSON includes **`conversation_id: conv.id`**. **`normalizeUsageTokens`** reads **`input_tokens` / `output_tokens`** when Codex-style.
- **Gateway:** **`api_server._derive_chat_session_id`** now hashes **`conversation_id`** into the seed when **`X-Hermes-Session-Id`** is absent so two chats with the same first user line do not share **`api-{digest}`**. Bogus header values **`undefined` / `null` / `none` / `[object object]`** are cleared so the derive path can run.
- **Prompt size:** **`buildMessages`** project system block is shorter (same facts, fewer tokens than the old multi-paragraph template). You cannot honestly shrink provider counts below real **tools + system + history**; new chats still pay tool/schema overhead.
- **Project system once per Hermes session:** **`run_agent`** persists **core + ephemeral_system_prompt** into **`sessions.system_prompt`** when the session row has no stored prompt yet. **`buildMessages`** omits the ANIMUS project **system** block only when **`hermes_has_stored_system_prompt === true`**, **`hermes_stored_prompt_confirmed_session_id === session_id`**, and preflight/SSE agree with SQLite (see **`reconcileHermesStoredPromptFromServer`**). Non-stream responses include **`X-Hermes-Has-Stored-System-Prompt`**. New **`session_id`** clears **`hermes_has_stored_system_prompt`**, **`hermes_stored_prompt_confirmed_session_id`**, and **`hermes_project_session_primed`**.
- **Long-thread auto-summarize (010526):** With **`X-Hermes-Session-Id`**, the gateway replaces request history with **`state.db`** messages — trimming **`conv.messages`** in the browser alone does not shrink Hermes tokens. The PWA therefore assigns a **new** **`session_id`** after a successful **`POST /api/chat/summarize-context`**, stores **`context_digest`** / **`context_digest_at`**, and **`buildMessages`** prepends that digest into the first **`system`** (merged with the project system when present). **Each later compaction** sends transcript **A** = prior **`context_digest`** (cumulative in-chat summary for this conversation) + **B** = verbatim turns in the prefix slice to fold this pass + **C** = output contract; the model returns one replacement **A** (server **`_CONTEXT_SUMMARY_SYSTEM`**). This is **not** a substitute for project continuity files (**`project_history.md`**, **`project_status.md`**, **`repo_map.md`**, **`project_knowledge.md`**, notes, goal) — those keep updating through the normal agent/tool path. Tune thresholds in **`index.html`**: **`CONTEXT_AUTO_SUMMARIZE_UTF8`**, **`CONTEXT_AUTO_SUMMARIZE_MIN_MESSAGES`**, **`CONTEXT_AUTO_SUMMARIZE_KEEP_TAIL`**. Server model override: **`ANIMUS_CONTEXT_SUMMARY_MODEL`** in **`animus.env`**. On summarize failure, **`_suppress_auto_summary_until`** avoids hammering the endpoint for 2 minutes.
- **ANIMUS digest + SQLite (010526):** On the **first** gateway turn, **`run_agent`** merged the full client ephemeral (including the in-chat digest) into **`sessions.system_prompt`**. Later turns load that row into **`_cached_system_prompt`** **and** append **`ephemeral_system_prompt`** again → **duplicate digest** every message → large input-token jumps on tiny user text. **Fix:** strip digest markers (**`[In-chat session digest`** / legacy **`[Prior conversation summarized`**) from ephemeral **before** the first **`update_system_prompt`** merge — see **`hermes-agent/animus_chat_digest_persist.py`** + **`run_agent.py`**. **Validation:** `pytest tests/run_agent/test_animus_digest_persist_strip.py -o addopts=`. Restart **`hermes-gateway`** after deploy.

## Tokens tab — what “input” means (300426)

- Provider **input** tokens are the **entire prompt** the model saw for that completion: system instructions, tool definitions, and **full thread context**, not just the characters in the latest user bubble. Short replies can still sit on large input counts when history and tools are large.

## Token usage dict shape (300426)

- **`agent.usage_pricing.normalize_usage`:** usage objects from Codex/Responses or proxies may be **plain dicts** (e.g. JSON). Reading with `getattr(dict, "input_tokens", 0)` always returned **0**, so `run_agent` never incremented `session_*_tokens` and the gateway SSE finish chunk stayed all zeros → ANIMUS `token_usage.jsonl` / Tokens tab showed empty aggregates. Fix: `_usage_pick_int` / `_usage_pick_nested` / `_nested_pick_int` in **`hermes-agent/agent/usage_pricing.py`**; **`run_agent._codex_merge_stream_usage`** `_tok_sum` handles dicts too. Regression tests in **`hermes-agent/tests/agent/test_usage_pricing.py`**.
- **`run_agent.run_conversation` token ledger:** do not gate on `if response.usage` — **`None`** and **`{}`** are falsy and skipped the entire session accounting block. When **`normalize_usage`** still yields all zeros (common for subscription Codex), **`AIAgent._rough_token_pair_when_usage_absent`** supplies **rough** counts: **input** uses **`estimate_messages_tokens_rough`** **marginal** per internal API call (snapshot reset each `run_conversation` user turn; **first call capped 262144**, subsequent deltas capped **64k**) so ANIMUS totals do not sum full history on every tool iteration; **output** uses **`estimate_tokens_rough`** on assistant text / streamed deltas (ceiling chars÷4). A former **16k** first-call cap made later turns (missing provider usage) log ~16k input while the first turn still showed ~40k API usage — totals looked like a bogus “small second bump”.

## Compact project memory index (300426)

- **Token-light navigation source:** `agent.project_workspace.ensure_workspace_files()` now ensures `project_memory/index.json` exists for each project root, and `refresh_repo_map()` regenerates it. This makes refresh/onboarding/cron continuity runs maintain both human map (`repo_map.md`) and machine index (`project_memory/index.json`) together.
- **Existing projects stay fresh without manual map refresh:** `ensure_workspace_files()` now also refreshes `project_memory/index.json` when stale (mtime > 6h) or malformed (bad schema/missing lists), so normal ensure/bootstrap calls upgrade older projects automatically.
- **Existing projects auto-upgrade:** policy mirroring now injects a `hermes-project-memory-v1` block when absent, so old `AGENTS.md` / `CLAUDE.md` / `.cursorrules` files are upgraded by normal workspace refresh flows without manual edits.
- **When guiding future agents:** read `project_memory/index.json` first for targeted file picks, then read `repo_map.md` only if extra human context is needed; keep `project_history.md` as tail-only audit context.

## Skills availability across models (300426)

- **API-server hardening:** `hermes-agent/gateway/platforms/api_server.py` now force-adds the `skills` toolset in `_create_agent`, so `skills_list` / `skill_view` / `skill_manage` stay available even if `platform_toolsets.api_server` was saved without `skills`.
- **ANIMUS chat guidance flag:** `animus-chat/app/index.html` regular chat sends include `animus_skill_mode: true`; `animus-chat/server.py` consumes that flag and injects a concise system note reminding the model to reuse existing skills and create/update one with `skill_manage` when workflows repeat. Plan pipeline calls do not set the flag, so plan-stage “no tools” prompts are unaffected.

## Project bootstrap mirrors (300426)

- **New project governance sync:** `agent.project_workspace.ensure_workspace_files()` now ensures `AGENTS.md`, `CLAUDE.md`, and `.cursorrules` exist for each project root and keeps them mirrored from the project's existing policy source (priority: `AGENTS.md` → `CLAUDE.md` → `.cursorrules`).
- **Workspace continuity files:** the same bootstrap now creates `project_status.md` and `project_knowledge.md` when missing, in addition to history/map/goal files, so newly added projects start with the full continuity set.
- **Template override:** set `HERMES_CHAT_POLICY_TEMPLATE=/abs/path/to/template.md` to force a custom default policy body when a project has no existing governance file yet.
- **Refresh button parity:** `refresh_repo_map()` now runs an `ensure_workspace_files(..., generate_repo_map_if_missing=False)` pass first, so existing projects get missing continuity files created and stale governance mirrors re-synced even when the operator only triggers repo-map refresh.
- **Cron workdir parity:** `cron.scheduler.run_job()` now calls `ensure_workspace_files(Path(workdir), generate_repo_map_if_missing=False)` for `workdir` jobs before launching `AIAgent`, so scheduled runs inherit the same governance + continuity file repair path as project refresh/onboarding.
- **Workspace editor parity:** ANIMUS project workspace API/UI now treats `project_status` and `project_knowledge` as first-class editable files (not just auto-created files), so operators can maintain the continuity set directly from the sidebar without dropping to shell/file tools.
- **Help source of truth update:** `docs/animus-user-guide.md` now explicitly documents continuity auto-bootstrap, refresh-time governance/continuity repair, and cron `workdir` continuity enforcement; this keeps Help answers aligned with the token-saving navigation strategy (goal/status/knowledge/map first, broad scans second).

## Plan pipeline Stop (300426)

- **Clarification modal blocks the Plan toolbar:** **`#planClarifyModal`** full-screen overlay sits above **`#planStopBtn`**, so users could not cancel during question waits. **Fix:** footer button **`#planClarifyModalStop`** calls **`requestPlanPipelineStop()`** (sets **`planStopRequested`**, **`planAbortClarifyModalPromise()`**, **`planAbortController.abort()`**) — same path as toolbar Stop. Toolbar Stop unchanged for non-modal stages.
- **No header “Clear” on Plan:** Removed **`#planClearBtn`**; clearing local idea/output/draft is via saved-draft **−** ( **`planDraftDeleteBtn`** → **`planClearDraft()`** ) and listing row deletes. While a run is in flight, use **Stop** (toolbar or clarification modal footer), not a separate clear control.

## Project list order + merge (300426)

- **Sidebar reorder:** Each **`.project-item`** is **`draggable`** (gear **`draggable=false`** + **`dragstart` `preventDefault`** so Edit still works). Drop on another row; **top / bottom half** = insert before / after. **⋮⋮** is visual only (`pointer-events:none`). Order: **`save()`** + **`flushProjectsToServer()`** → **`POST /api/projects`**.
- **Server `server._merge_projects_onto_client`:** Incoming POST array order is canonical for shared keys; disk-only keys append in disk order — so reorder survives merge and multi-client.
- **Client `mergeProjectLists(server, local)`:** **Server array order first**, then local-only projects — so a second device/PWA picks up reorder after **`GET /api/projects`** (init + **`pullProjectsFromServer()`** on **`visibilitychange`** / bfcache **`pageshow`**).
- **`GET /api/projects/list-simple`:** Returns rows in **`projects.json`** order (no alphabetical sort) so cron dropdowns match the sidebar.
- **Projects list collapsed default:** **`hermes_sidebar_projects_collapsed_v1`** — **`'1'`** = collapsed, **`'0'`** = expanded; missing key → **collapsed**. Expanding once writes **`'0'`** so the choice sticks.
- **Add project control:** **`#addProjectBtn.add-project-btn`** — **`color: var(--accent)`**, **`font-weight: 800`**, **`background: transparent`** (not the New-chat filled pill).
- **Sidebar `stab--icon` SVGs:** **`overflow: visible`** on the **`<svg>`** avoids stroke clipping (Help **?** looked “cut” when the default SVG viewport clipped half the glyph).
- **Cron list actions:** **`renderCron()`** builds **`cron-btn cron-btn--icon`** buttons with inline SVGs; **`cronJobActionIconSvgs()`** returns shared strings. **Run now** = stick runner (head **`circle`** + torso + forward/back **arms** + bent front leg + rear leg). **Pause** when active / **Play** when paused (same **`pause`/`resume`** API as before). **Run now** uses **`innerHTML`** save/restore while loading (not **`textContent`**). **Add job:** **`#addCronBtn`** only — **`addCronBtnLabel`** removed; **`.cron-jobs-section-label .add-project-btn`** shares Projects **+** styling.
- **PWA / desktop settings sync:** **`POST /api/animus/client-config`** accepts **`{ ui_settings }`** (full **`settings`** snapshot, JSON-safe, ~450KB cap) → **`config.json`** key **`animus_ui_settings`**; **`GET`** returns **`ui_settings`**. Client: **`applyAnimusClientConfigFromJson`** merges **`ui_settings`** then legacy top-level fields; **`save()`** debounces **`schedulePersistUiSettingsToServer`** (~900ms); **`pullClientConfigFromServer()`** on **`visibilitychange`** (with convs/projects). **`_applyingServerClientConfig`** skips pushing while applying server JSON. Legacy **`wake_lock`**, **`inference_models`**, cron/TTS POSTs still update **`animus_ui_settings`** subsets on the server.

## Default **General** project (300426)

- **`server.ensure_animus_general_project()`** (in **`animus-chat/server.py`**): creates **`<projects_sync_root>/general`** and appends a **`projects.json`** row (stable id **`00000000-0000-4000-8000-000000000001`**, display name **General**) when no row already points at that resolved path. Runs at **lifespan startup**, on **`POST /api/animus/client-config`** when **`projects_dir`** is saved, and from the wizard after **`POST /api/setup/save-config`** / **`POST /api/setup/complete`** when **`projects_dir`** is non-empty (via **`importlib.import_module("server")`** in **`wizard_routes.py`**).
## Skills availability across models (300426)

- **API-server hardening:** `hermes-agent/gateway/platforms/api_server.py` now force-adds the `skills` toolset in `_create_agent`, so `skills_list` / `skill_view` / `skill_manage` stay available even if `platform_toolsets.api_server` was saved without `skills`.
- **ANIMUS chat guidance flag:** `animus-chat/app/index.html` regular chat sends include `animus_skill_mode: true`; `animus-chat/server.py` consumes that flag and injects a concise system note reminding the model to reuse existing skills and create/update one with `skill_manage` when workflows repeat. Plan pipeline calls do not set the flag, so plan-stage “no tools” prompts are unaffected.

## Project bootstrap mirrors (300426)

- **New project governance sync:** `agent.project_workspace.ensure_workspace_files()` now ensures `AGENTS.md`, `CLAUDE.md`, and `.cursorrules` exist for each project root and keeps them mirrored from the project's existing policy source (priority: `AGENTS.md` → `CLAUDE.md` → `.cursorrules`).
- **Workspace continuity files:** the same bootstrap now creates `project_status.md` and `project_knowledge.md` when missing, in addition to history/map/goal files, so newly added projects start with the full continuity set.
- **Template override:** set `HERMES_CHAT_POLICY_TEMPLATE=/abs/path/to/template.md` to force a custom default policy body when a project has no existing governance file yet.
- **Refresh button parity:** `refresh_repo_map()` now runs an `ensure_workspace_files(..., generate_repo_map_if_missing=False)` pass first, so existing projects get missing continuity files created and stale governance mirrors re-synced even when the operator only triggers repo-map refresh.
- **Cron workdir parity:** `cron.scheduler.run_job()` now calls `ensure_workspace_files(Path(workdir), generate_repo_map_if_missing=False)` for `workdir` jobs before launching `AIAgent`, so scheduled runs inherit the same governance + continuity file repair path as project refresh/onboarding.
- **Workspace editor parity:** ANIMUS project workspace API/UI now treats `project_status` and `project_knowledge` as first-class editable files (not just auto-created files), so operators can maintain the continuity set directly from the sidebar without dropping to shell/file tools.
- **Help source of truth update:** `docs/animus-user-guide.md` now explicitly documents continuity auto-bootstrap, refresh-time governance/continuity repair, and cron `workdir` continuity enforcement; this keeps Help answers aligned with the token-saving navigation strategy (goal/status/knowledge/map first, broad scans second).

## Plan pipeline Stop (300426)

- **Clarification modal blocks the Plan toolbar:** **`#planClarifyModal`** full-screen overlay sits above **`#planStopBtn`**, so users could not cancel during question waits. **Fix:** footer button **`#planClarifyModalStop`** calls **`requestPlanPipelineStop()`** (sets **`planStopRequested`**, **`planAbortClarifyModalPromise()`**, **`planAbortController.abort()`**) — same path as toolbar Stop. Toolbar Stop unchanged for non-modal stages.
- **No header “Clear” on Plan:** Removed **`#planClearBtn`**; clearing local idea/output/draft is via saved-draft **−** ( **`planDraftDeleteBtn`** → **`planClearDraft()`** ) and listing row deletes. While a run is in flight, use **Stop** (toolbar or clarification modal footer), not a separate clear control.

## Project list order + merge (300426)

- **Sidebar reorder:** Each **`.project-item`** is **`draggable`** (gear **`draggable=false`** + **`dragstart` `preventDefault`** so Edit still works). Drop on another row; **top / bottom half** = insert before / after. **⋮⋮** is visual only (`pointer-events:none`). Order: **`save()`** + **`flushProjectsToServer()`** → **`POST /api/projects`**.
- **Server `server._merge_projects_onto_client`:** Incoming POST array order is canonical for shared keys; disk-only keys append in disk order — so reorder survives merge and multi-client.
- **Client `mergeProjectLists(server, local)`:** **Server array order first**, then local-only projects — so a second device/PWA picks up reorder after **`GET /api/projects`** (init + **`pullProjectsFromServer()`** on **`visibilitychange`** / bfcache **`pageshow`**).
- **`GET /api/projects/list-simple`:** Returns rows in **`projects.json`** order (no alphabetical sort) so cron dropdowns match the sidebar.
- **Projects list collapsed default:** **`hermes_sidebar_projects_collapsed_v1`** — **`'1'`** = collapsed, **`'0'`** = expanded; missing key → **collapsed**. Expanding once writes **`'0'`** so the choice sticks.
- **Add project control:** **`#addProjectBtn.add-project-btn`** — **`color: var(--accent)`**, **`font-weight: 800`**, **`background: transparent`** (not the New-chat filled pill).
- **Sidebar `stab--icon` SVGs:** **`overflow: visible`** on the **`<svg>`** avoids stroke clipping (Help **?** looked “cut” when the default SVG viewport clipped half the glyph).
- **Cron list actions:** **`renderCron()`** builds **`cron-btn cron-btn--icon`** buttons with inline SVGs; **`cronJobActionIconSvgs()`** returns shared strings. **Run now** = stick runner (head **`circle`** + torso + forward/back **arms** + bent front leg + rear leg). **Pause** when active / **Play** when paused (same **`pause`/`resume`** API as before). **Run now** uses **`innerHTML`** save/restore while loading (not **`textContent`**). **Add job:** **`#addCronBtn`** only — **`addCronBtnLabel`** removed; **`.cron-jobs-section-label .add-project-btn`** shares Projects **+** styling.
- **PWA / desktop settings sync:** **`POST /api/animus/client-config`** accepts **`{ ui_settings }`** (full **`settings`** snapshot, JSON-safe, ~450KB cap) → **`config.json`** key **`animus_ui_settings`**; **`GET`** returns **`ui_settings`**. Client: **`applyAnimusClientConfigFromJson`** merges **`ui_settings`** then legacy top-level fields; **`save()`** debounces **`schedulePersistUiSettingsToServer`** (~900ms); **`pullClientConfigFromServer()`** on **`visibilitychange`** (with convs/projects). **`_applyingServerClientConfig`** skips pushing while applying server JSON. Legacy **`wake_lock`**, **`inference_models`**, cron/TTS POSTs still update **`animus_ui_settings`** subsets on the server.

## Default **General** project (300426)

- **`server.ensure_animus_general_project()`** (in **`animus-chat/server.py`**): creates **`<projects_sync_root>/general`** and appends a **`projects.json`** row (stable id **`00000000-0000-4000-8000-000000000001`**, display name **General**) when no row already points at that resolved path. Runs at **lifespan startup**, on **`POST /api/animus/client-config`** when **`projects_dir`** is saved, and from the wizard after **`POST /api/setup/save-config`** / **`POST /api/setup/complete`** when **`projects_dir`** is non-empty (via **`importlib.import_module("server")`** in **`wizard_routes.py`**).
- **Client:** **`findAnimusDefaultGeneralProject()`** matches **`PROJECTS_ROOT + '/general'`** (case-insensitive, slashes normalised). After **`syncProjectsFromRoot()`**, if found and **`sessionStorage`** lacks **`animus_general_workspace_session_v1`**, sets it and calls **`enterProjectView`** so each **browser session** starts in General once.

## ANIMUS chat ghost branding (300426)

- **`animus-chat/app/index.html`:** **Sidebar** **`.sidebar-head .logo`** uses **`ghostonlyicon`** with **`.brand-ghost--sidebar`** (**56×56**, 2× the old 28px mark). **Main** top bar (**`.header`**) has **no** ghost image — only **`#headerTitle`** text plus menu / New chat (avoids a PNG on the same strip as New chat). Empty chat uses **`.empty-brand-ghost`** in **`#emptyState`**. After shell HTML or **`sw.js`** cache changes, bump **`CHAT_SERVER_REV`** in **`server.py`** and **`CACHE`** in **`app/sw.js`** together.

## Settings model list missing OpenAI Codex (300426)

- **`GET /api/models`** builds from **`animus-chat/data/hermes_models_cache.json`** when present. A cache saved from the **gateway** OpenAI **`/v1/models`** shape is mostly **`owned_by: openai`** rows → early return in **`_models_ui_rows()`** used to **drop** entire providers such as **`openai-codex`**, **`cursor-agent`**, and **`claude-code`**, so Settings inference matrix model `<select>`s looked empty for Codex. **Fix:** **`_models_ui_fill_missing_providers()`** appends Hermes CLI catalog rows for any **`_UI_MODEL_PROVIDERS`** entry absent from the cached set before returning.

## Settings → Claude Code sign-in (300426)

- Inference matrix **Claude Code** row: **Sign in (server)** calls **`POST /api/setup/claude-code-login-start`**, which **`Popen`**s **`claude setup-token`** on the ANIMUS host (same entrypoint as Hermes **`agent/anthropic_adapter.run_oauth_setup_token`** / dashboard **`cli_command`**). Browser/TTY prompts run **on the server**; use **Re-check** to refresh **`provider-status`**. Requires **`claude`** on **`PATH`** for the chat service (install **`@anthropic-ai/claude-code`** globally, then **`animus restart`** if PATH changed).

## ANIMUS CLI on PATH (300426)

- **`scripts/animus`**: **`animus start`**, **`animus stop`**, **`animus restart`**, **`animus status`** → **`systemctl --user`** on **`animus.service`**. **`./scripts/sync-dev-systemd.sh`** symlinks it to **`~/.local/bin/animus`** and **`chmod +x`** the script. If the shell says **command not found**, ensure **`export PATH="${HOME}/.local/bin:${PATH}"`** (e.g. in **`~/.profile`**). Override unit: **`ANIMUS_SYSTEMD_UNIT=other.service animus restart`**.

## Models cache + matrix ids (300426)

- **`_normalize_ui_model_provider()`** maps gateway **`owned_by`** (e.g. **`gemini`**, **`togetherai`**) to ANIMUS Settings matrix ids (**`google`**, **`together`**) so `<select>` filters match **`INFERENCE_MATRIX_ORDER`**. **`POST /api/models/refresh`** merges **Hermes curated CLI rows + Cursor CLI + live `GET {HERMES_API_URL}/v1/models`**, then **`_models_ui_fill_missing_providers`** — response includes **`gateway_models_merged`** count. Inference matrix: **OAuth / Cursor** rows allow **changing the default model while `not_signed_in`** (saves `inference_models`; chat still needs sign-in). Codex poll UI always includes a clickable **Open sign-in page** link (popup-blocker fallback). Cursor row shows **`inf-cursor-status`** + JSON error body from **`cursor-login-start`**.

## Read aloud → Piper radio (300426)

- **`renderTtsSettings`** used to **`pr.disabled = !hasPiper`** and force **`settings.tts_backend = 'browser'`** when **`GET /api/tts/backends`** did not list `piper`. If that request fails (network / server restart), **`hasPiper`** was false and users could not pick Piper. **Fix:** Piper is always selectable; probe is **tri-state** (`true` / `false` / `null` unknown); no silent downgrade of saved `tts_backend`. **`tryReadAloudPiper`** only checks `settings.tts_backend === 'piper'`.
- **Conversation mode loop retry (300426):** **`listenConversationTurnAndSend`** now re-queues listening after empty STT transcript (short delay) and after listener errors (longer delay) while mode is still active. This prevents hands-free mode from stalling after silence-only captures or transient microphone/STT failures. **`stopConversationMode`** / **`startConversationMode`** clear the retry timer to avoid duplicate loops.
- **Conversation mode + STT (300426):** Hands-free mode always uses **`MediaRecorder`** → **`POST /api/stt/transcribe`** (not Web Speech). **On-device:** **`HERMES_CHAT_STT_LOCAL_EMBEDDED=1`** runs **faster-whisper** in-process (via **`hermes-agent/tools/transcription_tools.py`** → **`transcribe_audio_force_local_faster_whisper`**, ignores Hermes **`stt.provider`**); default model **`HERMES_CHAT_STT_LOCAL_MODEL`** = **`small`**. Order: **`HERMES_CHAT_STT_LOCAL_URL`** → embedded → cloud keys. **`faster-whisper>=1.0.0,<2`** is in **`animus-chat/requirements.txt`** (shipped in buyer zip; **`install.sh`** / manual venv both use it); **`build-release.sh`** greps for **`faster-whisper`** so it cannot ship without the line. **`ffmpeg`** helps webm/mp4 decode. Alt: **`HERMES_CHAT_STT_LOCAL_URL`** / **`OPENAI_API_KEY`** in **`animus.env`**. **`GET /api/hermes-chat-meta`** → **`stt_backend`**. **`index.html`:** **`refreshSttBackendFromMeta()`** runs at start of **`renderTtsSettings()`** and **`onVoiceInputClick()`** so Read aloud / mic see updated STT after restart without a full tab reload; init uses **`applySttBackendFromMeta(meta)`** + **`cache: 'no-store'`** on meta fetch. Hint when **`none`**: ensure **`animus.env`** line is **not** commented with **`#`**. See **`animus.env.example`**, **`docs/tts.md`**, **`docs/hermes-agent-patches.md`** Patch 10.
- **Conversation mode mic icon (300426):** When Settings → **Conversation mode** is enabled, **`refreshVoiceInputButtonState`** swaps **`#voiceInputBtn`** inner SVG to a **talking-head** (person + sound arcs); when disabled, **mic** SVG. **`dataset.voiceIconMode`** avoids redundant DOM writes. Toggle **change** always calls **`refreshVoiceInputButtonState`** (including on disable after **`stopConversationMode`**).
- **Conversation mode TTS after 1st reply (300426):** **`maybeContinueConversationAfterAssistant`** holds **`conversationLoopRunning`** across **`await listenConversationTurnAndSend()`** → **`await send()`**. **`send()`** **`finally`** calls **`maybeContinueConversationAfterAssistant(assistantText)`** while the flag is still **true**, so the inner call used to set only **`conversationAutoListenQueued`** and **return** — **assistant text was never spoken**; outer **`finally`** then called **`maybeContinue('')`** (listen-only). **Fix:** replace the boolean queue with **`conversationDeferredAssistantTts`** (latest non-empty assistant string); outer **`finally`** runs **`maybeContinue(deferred)`** so Piper / Web Speech runs for every turn.
- **Conversation mode + inference switch (300426):** **`send()`** only set **`conversationReplyText`** inside **`if (full)`**, so an **empty** assistant body (or a failed stream where **`conversationReplyText`** was never set) skipped **`maybeContinueConversationAfterAssistant`** — hands-free mode never resumed listening/TTS. **Fix:** assign **`conversationReplyText`** from **`endStream()`** / catch **`partial`** every time; **`finally`** calls **`maybeContinueConversationAfterAssistant`** whenever conversation mode is **active** (pass empty string → TTS skips, **listen** still runs).
- **Read aloud Settings layout:** **`#animusSttSourceRadios`** mirrors **`#ttsBackendRadios`** CSS so **`.field input{width:100%}`** does not stretch STT radios; STT vs TTS sections have separate titles (**Speech-to-text** / **Read-aloud voice**).
- **ANIMUS embedded STT speed:** **`transcribe_audio_force_local_faster_whisper`** passes **`beam_size`** from **`HERMES_CHAT_STT_BEAM_SIZE`** (default **1**; Hermes messaging **`_transcribe_local`** unchanged at **5**). Smaller **`HERMES_CHAT_STT_LOCAL_MODEL`** (**`tiny`** / **`base`**) still dominates CPU time.
- **Release zip size:** **`./build-release.sh`** excludes **`hermes-agent/.venv/*`** (local Python env, often tens of MB) in addition to **`hermes-agent/venv/*`**. If a dev venv lives under **`.venv`**, omit it from buyer zips or the **≤55MB** cap fails. Also exclude stray **`animus-chat/${HOME}/`** (bad path) + **`animus-chat/whoami`** if present.
- **Projects sidebar order:** **`mergeProjectLists`** used to always apply **server array order first**, so **`visibilitychange`** / init **`GET /api/projects`** could run **before** the debounced **`POST /api/projects`** finished and **revert drag reorder**. **Fix:** **`_projectsOrderDirty`** + **`sessionStorage`** **`animus_projects_order_dirty_v1`** after **`reorderProjectsByDrop`**; merge with **`'local'`** preference while dirty; clear on successful **`flushProjectsToServer`** / **`_serverSync`** projects POST. **`pullProjectsFromServer`** and init use the same rule.
- **localhost vs 127.0.0.1 (ERR_FAILED):** **`server.py`** defaults **`CHAT_HOST`** to **`::`** when unset/blank. Plain **`uvicorn.run(host="::")`** often leaves **`IPV6_V6ONLY=1`**, so **`127.0.0.1:PORT`** stops working. **`_prebound_socket_for_chathost`** binds **`::`** with **`IPV6_V6ONLY=0`** then **`UvicornServer(...).run(sockets=[sock])`** so **localhost**, **::1**, and **127.0.0.1** all work when the OS allows dual-stack. **`CHAT_HOST=127.0.0.1`** still IPv4-only — use **`127.0.0.1`** in the URL or switch to **`::`**. Startup **Tip** / **`::` warning** unchanged; **`create-desktop-launcher.sh`** maps **`::`** / **`0.0.0.0`** → **`127.0.0.1`** for the `.desktop` URL.
- **STT multipart:** Starlette returns **`bad_form`** / *`python-multipart` library must be installed* if **`python-multipart`** is missing from the chat venv — required for **`POST /api/stt/transcribe`** and **`POST /api/attachment/text`**. Listed in **`animus-chat/requirements.txt`** (same file **`install.sh`** / Docker **`Dockerfile`** use); **`build-release.sh`** greps for the package name; **`install.sh`** runs **`python -c "import multipart"`** after **`pip install -r`** so buyer zips fail fast if the tree is broken.
- **Conversation mode mic (listening):** **`#voiceInputBtn`** uses **`.conversation-on.recording`** for a slow purple **laser** ring (**`::before`** + **`conic-gradient`** + **`micLaserSpin`** + radial **mask**). Plain recording uses **`.recording:not(.conversation-on)`** so the original red **`micPulse`** is unchanged when conversation mode is off.
- **Settings STT source (no env edit):** **Local** = **`animus_chat_stt_source: "embedded"`** in **`config.json`** via **`POST /api/animus/client-config`** (same effective path as embedded STT for transcribe when no **`HERMES_CHAT_STT_LOCAL_URL`**). **Online** = **`"openai"`** + optional **`animus_chat_stt_openai_key`** / **base** / **model**. Client: **`postAnimusChatSttSource`**, **`#animusSttOnlineSaveBtn`** / **`#animusSttOnlineClearKeyBtn`**; do not put the API key in **`ui_settings`** (server strips on sanitize). After changing source or keys, **`refreshSttBackendFromMeta()`** + **`renderTtsSettings()`** keep **`stt_backend`** label in sync.

## Settings → Restart gateway (300426)

- **`POST /api/restart/gateway`** used to run **synchronously** (Hermes dashboard HTTP up to **60s**, then **`hermes gateway restart`** up to **60s**, then systemd). Browsers and reverse proxies often treat that as **“Failed to fetch”** (connection closed / timeout). **Fix:** handler returns **`{ ok, scheduled: true }`** immediately and runs the same chain in **`BackgroundTasks`**; dashboard restart uses **`dashboard_post_gateway_restart(timeout=12)`**.

## Settings → Messaging → Hermes (300426)

- **ANIMUS `messaging_routes.py`:** **`GET /api/messaging/overview`** (platform catalog + enabled/connected flags), **`GET/POST /api/messaging/platform/{id}`** (forms keyed by Hermes **`Platform`** slug). Saves use **`hermes_cli.config.save_env_value` / `remove_env_value`** and **`read_raw_config` + `save_config`** for **`platforms.<id>.enabled`** and **`home_channel`** (secrets stay in **`.env`**, not duplicated in YAML). **`is_managed()`** → read-only + **403** on save. Legacy **`GET /api/integrations/hermes-gateway/*`** unchanged for old clients.

## Messaging Slack import from animus.env (300426)

- **`POST /api/messaging/import-animus-slack`**: if repo-root **`animus.env`** contains **`SLACK_BOT_TOKEN`** and **`load_env()`** shows Hermes **`~/.hermes/.env`** has **no** **`SLACK_BOT_TOKEN`**, copies token + optional **`SLACK_DEFAULT_CHANNEL`** → **`SLACK_HOME_CHANNEL`**, enables **`platforms.slack`** in **`config.yaml`**. **`index.html`** calls it once per **`renderMessagingSettingsPanel()`** load (re-entrant second pass skips import). Cron **Slack** delivery still uses **`SLACK_WEBHOOK_URL`** in **`animus.env`** only — **`integrations_slack`** routes remain for save/status/test of webhook vars.

## Hermes agent surfaces vs ANIMUS (300426)

- **ANIMUS chat proxy:** `POST /api/chat` → gateway `POST /v1/chat/completions` (SSE); forwards `X-Hermes-Session-Id`; `GET /api/models` + raw `?gateway=1`; probes `GET /v1/models`, `GET …/health/detailed`. **Cron** uses gateway **`/api/jobs`**. Still no ANIMUS proxy for **`/v1/responses`** / **`/v1/runs`** unless added later.
- **Hermes dashboard (`hermes dashboard`, `web_server.py`):** REST for config/env/schema, OAuth provider flows, sessions CRUD + search, logs, cron jobs, skills toggle, toolsets, analytics usage, PTY TUI WebSocket, pub/sub/events WebSockets, dashboard themes/plugins — mostly **not** duplicated in ANIMUS PWA (ANIMUS has its own setup wizard, cron/skills routes, tokens, etc.).
- **Full inventory for “what could we wire?”** — see agent chat reply listing gateway OpenAI-compat extras, dashboard APIs, CLI subcommands, core tools/toolsets, plugins (memory/context/image-gen), messaging platforms, ACP/TUI/batch — sourced from `hermes-agent/gateway/platforms/api_server.py`, `hermes_cli/web_server.py`, `hermes_cli/main.py`, `toolsets.py`, `hermes-agent/AGENTS.md`.

## Hermes dashboard token for ANIMUS server-side REST (300426)

- Dashboard gates **`/api/*`** with ephemeral **`X-Hermes-Session-Token`** (also `Authorization: Bearer …`). Token is **`window.__HERMES_SESSION_TOKEN__`** in the dashboard browser console (`hermes dashboard`). Set **`HERMES_DASHBOARD_SESSION_TOKEN`** (+ optional **`HERMES_DASHBOARD_URL`**, default `http://127.0.0.1:9119`) in **`animus.env`** so ANIMUS can call **`POST /api/gateway/restart`**, **`GET/PUT`** skills + analytics, and mirror model **`PUT /api/config`**. Without the token, ANIMUS uses in-process Hermes modules for skills toggle/list and skips Hermes session analytics merge; gateway restart uses **`hermes gateway restart`** then systemd.
- Cron in ANIMUS prefers gateway **`GET/POST/PATCH/DELETE /api/jobs`** on **`HERMES_API_URL`** (same **`Authorization`** as chat). List falls back to **`cron.jobs`** if the gateway call is not HTTP 200 with **`jobs`**.

## Release zip (290426)

- **`build-release.sh`** excludes **`animus-chat/animus.env`** (root `animus.env` was already excluded), **`hermes-agent/.env`**, **`hermes-agent/.envrc`**, **`*.flock`**, **`./animus-update-server/*`**, and **repo-root `./scripts/*`** only (not **`hermes-agent/.../scripts/`**). After zipping, the script **fails** if the archive lists a raw `animus.env`, raw `hermes-chat.env`, raw **`hermes-agent/.env`**, **`*.flock`**, **`animus-chat/data/`**, or any path **`animus-update-server/`**. `.gitignore` lists **`animus-chat/animus.env`** and **`hermes-agent/.env`** so they are less likely to be committed.
- **`animus-site/`** (sibling repo, not in buyer zip): Vercel **`vercel.json` `version`: 2** uses **`@vercel/static`** for root **`*.html`** (includes **`seller-publish.html`** — avoid **`/admin/*`**-only static globs; they can 404 on some deploys) + **`css/**`**, **`js/**`**, **`assets/**`** + **`@vercel/python`** for **`api/*.py`** + **`@vercel/node`** for **`api/release_upload.js`**; optional **`redirects`** from legacy **`/admin/release.html`**. **`package.json`** for **`@vercel/blob`**. **`routes`**: **`/api/latest.json`**, **`/updates/latest.json`**, **`/api/admin/*`**, **`/api/history`**. No **`runtime.txt`** (Python **3.12** in build logs). **Upstash Redis:** Vercel often sets **`REDIS_URL`** only — wire password **≠** REST token; **`lib_redis.py`** uses **`redis`** TCP (**`Redis.from_url`**) for **`REDIS_URL`**. If **`UPSTASH_REDIS_REST_URL`** + **`UPSTASH_REDIS_REST_TOKEN`** (or **`KV_REST_API_*`**) exist, **`upstash_redis`** REST is used instead. **`/api/latest`** uses short CDN **`Cache-Control`** so manifests are not stuck at **`0.0.0`**. Set **`ANIMUS_UPDATE_URL`** in **`animus.env.example`** to **`https://<project>.vercel.app/api/latest.json`** after deploy.
- **animus-site design source of truth (290426):** Use **`animus-chat/app/index.html`** and **`docs/animus-user-guide.md`** for marketing claims; the actual product surfaces are Chats / Skills / Cron / Plan / Settings, first-run wizard, provider matrix, Projects, SSH/Slack, token usage, Piper/browser TTS, Tailscale PWA, and manifest zip updates. Preview art lives in **`/home/sketch/animus-site/assets/screenshots/{chat,wizard,settings,cron}.svg`** and should stay aligned with those real surfaces rather than generic placeholders.
- **animus-site release notes (290426):** Keep **`updates.html`** with static fallback release content so buyers never see permanent **Loading...** if browser fetches fail; live API enhancement lives in external **`js/updates.js`** (external scripts were confirmed to run where the inline release fetch could leave the DOM unchanged). Validate with `curl -L https://animusai.vercel.app/updates.html | rg "Loading|v1.0.0"` and Chromium `--dump-dom`.
- **Publish buyer updates (300426):** UI: **`animus-site/seller-publish.html`** (linked from **`docs.html`**; legacy **`/admin/release.html`** redirects) — **`ADMIN_TOKEN`**, semver, zip → Vercel Blob (needs **`BLOB_READ_WRITE_TOKEN`**) + **`POST /api/admin/publish`**. The Blob SDK’s token **`fetch`** sends **no** admin header; **`api/release_upload.js`** requires a prior **`release-challenge`** (admin header) returning HMAC **`clientPayload`**, verified in **`onBeforeGenerateToken`**. CLI/static: **`./scripts/release-and-publish.sh`** copies zip to **`releases/`** + **`vercel --prod`**, then **`./scripts/publish-animus-manifest.sh`** (after **`vercel env pull …`** + **`set -a && source …`** if needed). **`ADMIN_TOKEN`**: if **`env pull`** is empty, token was **sensitive** — **`vercel env add …`** or dashboard + **redeploy**. **`animusai.vercel.app`** must **alias** Production or **`/releases/*`** / admin publish can 404/403 while **`/api/latest.json`** still updates (shared Redis).

- **animus-site release notes + history (300426):** **`POST /api/admin/publish`** requires **`notes`** ≥ **24** chars and rejects version-only strings (**`lib_release.validate_publish_notes`**). **`./scripts/publish-animus-manifest.sh`** requires **`ANIMUS_RELEASE_NOTES`** (same length). **`GET /api/history`** dedupes by semver (**`lib_release.dedupe_history_entries`**); each publish and **`POST /api/admin/history`** call **`compact_redis_history`** so Redis **`animus:history`** stays one row per version (richest **`notes`** wins on duplicates). Deploy sibling **`animus-site/`** after changing **`api/*.py`**. **`vercel alias set <deployment-url> animusai.vercel.app`** has no **`--yes`** flag (CLI errors if added). One-shot Redis compact: `curl -sS -X POST "https://animusai.vercel.app/api/admin/history" -H "x-admin-token: …"` (needs **`ADMIN_TOKEN`** locally or in **`seller-private/ADMIN_TOKEN`**).

- **animus-site deploy vs hostname (300426):** **Buyers and docs use only **`https://animusai.vercel.app`**.** Do not ship **`animus-site-*.vercel.app`** or project default URLs in **`animus.env.example`**, Gumroad copy, or README examples. After **`vercel --prod`**, run **`vercel alias set <deployment-url-from-output> animusai.vercel.app`** from **`animus-site/`** so marketing + **`/releases/*`** + APIs stay on the buyer hostname. Quick check: `curl -L https://animusai.vercel.app/` should match current Production HTML.
- **≤55MB cap:** the script **exits 1** if the zip exceeds **55MB**. Trims from the zip (not from your working tree): **`Ghost3D/*`**, **`hermes-agent/tests/*`**, **`hermes-agent/website/*`**, **`hermes-agent/scripts/whatsapp-bridge/node_modules/*`**, **`hermes-agent/.cursor/*`**. WhatsApp bridge: **`INSTALL.md`** → run `npm ci` in `hermes-agent/scripts/whatsapp-bridge/` when using that feature.
- **No public repo link in PWA:** About ANIMUS points to the **Help** sidebar tab. **Updates:** optional **`ANIMUS_UPDATE_URL`** in **`animus.env`**; if unset, **`check-updates`** / **`apply-update`** use **`https://animusai.vercel.app/api/latest.json`**. JSON manifest (`version`, `download_url`, `notes`); **httpx** (no git). Seller may use sibling **`animus-site`** or **`animus-update-server/`**; **`animus.env.example`** can still document a custom URL. Zip extract: **zip-slip** guard in **`server.py`**.
- **Buyer zip:** omits **ANIMUS internal continuity** (`project_*.md`, `repo_map.md`, `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `.cursor/`, `setup_repo.md`, `animus-chat/repo_map.md`, `animus-chat/project_history.md`, `animus-chat/setup_repo.md`) and **`hermes-agent/AGENTS.md`** + **`hermes-agent/.cursor/`**. **Clone the repo** for continuity files; the UI still refers to per-project **`project_goal.md`** files on disk, not the monorepo north-star file.

## Gumroad zip (290426)

- **`seller-private/` (300426):** Repo-root folder for **seller-only** local files (e.g. **`ADMIN_TOKEN`** one-liner). **`.gitignore`:** everything under **`seller-private/`** except **`README.md`**. **`build-release.sh`:** **`-x ./seller-private/*`** plus zip leak check so buyers never get it. Not encryption — use **`chmod 700`**, OS disk encryption, and/or a password manager for real secrecy.
- **`animus.env` bootstrap (300426):** **`installer/install.sh`** already created **`animus.env`** from **`animus.env.example`** when missing; **`installer/preflight.sh`** now does the same so early runs are consistent. Docker image bakes **`/app/animus.env`** from **`animus.env.example`** at build time.
- **Buyers:** point to root **`START_HERE.txt`** then **`INSTALL.md`**. Release artifact remains **`./build-release.sh`** → `animus-v$(cat VERSION | tr -d '[:space:]').zip`.
- **Sellers:** **`docs/GUMROAD.md`** — pre-upload checklist, suggested bullets, thank-you email snippet.
- **Gumroad ≠ auto-push:** Uploading a new product file does **not** download to buyers’ disks; they re-download from Library / receipt link; seller email helps.

## First-run wizard (290426)

- **`/api/setup/status`** and **`GET /api/animus/client-config`** use **`cfg_still_first_run()`** in `setup_wizard/wizard_routes.py` so `"false"` / `0` in `config.json` do not keep re-opening the wizard.
- **`POST /api/setup/complete`** sets `first_run: false` plus **`setup_completed_at`** (UTC). The PWA also calls complete when the **final wizard summary** is shown so refresh before “Open ANIMUS” does not loop the wizard.

## Skills list empty on fresh ANIMUS (290426)

- **`GET /api/skills/list`** uses in-process **`tools.skills_tool._find_all_skills`**, which scans **`HERMES_HOME/skills`** (and `config.yaml` **`skills.external_dirs`** only). Bundled catalog lives under **`hermes-agent/skills/`** until **`tools.skills_sync.sync_skills()`** copies them into **`HERMES_HOME/skills`**.
- The **Hermes CLI** runs **`sync_skills(quiet=True)`** on every **`hermes`** launch (`hermes_cli/main.py`). **ANIMUS** is often started only via **`animus-chat/server.py`**, so new profiles stayed empty until **`server.py`** was taught to call **`sync_skills`** once at import (after **`HERMES_AGENT`** is on **`sys.path`**).
- **Wizard still showed 0 (300426):** import-time sync can fail silently (IO, bad **`HERMES_AGENT_DIR`**, etc.); the wizard treated non-array JSON as empty. **`skills_routes.skills_list_api`** now runs **`sync_skills(quiet=True)`** when **`HERMES_HOME/skills/.bundled_manifest`** is missing, and wizard step 4 surfaces **`error`** / HTTP status when the list response is not an array.
- **Skills vs “Check for updates” (300426):** **`POST /api/animus/check-updates`** is the **ANIMUS zip** updater, not Hermes skills. **`POST /api/skills/update-all`** runs **`hermes skills update`** (CLI) and only updates **already-installed** skills; it does not replace in-process **`sync_skills`** seeding. If the skills dir never got copies, fix **`HERMES_AGENT_DIR`** so **`hermes-agent/skills/`** exists, restart chat, then reload Skills — seed logic also retries when **`.bundled_manifest` exists but is empty** and there is no **`SKILL.md`** under **`HERMES_HOME/skills`** while bundled skills are present.
- **Monorepo + systemd (300426):** Run **`./scripts/sync-dev-systemd.sh`** so **`animus.service`** uses **`%h/animus/animus-chat`** and **`animus.env`** has **absolute** **`HERMES_AGENT_DIR`** / **`HERMES_HOME`** (`${HOME}` in **`EnvironmentFile`** is not expanded — Hermes paths break). Stop any manual **`python server.py`** on **`CHAT_PORT`** before **`systemctl --user restart animus.service`** or the unit exits with “address already in use”. **`animus-chat/restart-after-code-change.sh`** prefers **`animus.service`** over **`hermes-chat.service`**.

## Chat UI assets (290426)

- **`ghostonlyicon.png`:** Served as **`/ghostonlyicon.png`** from `animus-chat/app/`. Used for empty chat, sidebar + main header brand, favicon, `manifest.json` icons, apple-touch-icon, About modal, notification icon/badge, and Linux `.desktop` icon preference (`server.py` desktop launcher + `installer/create-desktop-launcher.sh` try this file before `icon-192.png`). Service worker cache key **`animus-v13`** (bump **`sw.js`** `CACHE` when shell looks stale; hard reload or **`?purge=1`** once). **`register(...).then(r => r.update())`** nudges SW updates on each load.
- **Plan tab (300426):** In-tab intro paragraph removed. Resumable draft row shows **`draftLabelAt`/`savedAt`** as **`HHMM MMDDYY`** (matches Hermes **`project_workspace`** one-line stamp) + one-line **`ideaRaw`**; **✎** scrolls to **`#planIdeaInput`**, **▶** resumes **`runPlanPipeline({ resume: true })`**, **−** clears **`PLAN_DRAFT_KEY`**. Clarification uses **`#planClarifyModal`** (not the scroll stack): **`planWaitUserClarification({ title, questions, legacyMatchPrompt, answerKey })`** returns **`{ cancelled, text }`**; **Cancel** / Esc saves **`clarifyAnswers`/`gapClarifyAnswers`** via **`planTouchClarifyDraft`**; **Stop** calls **`planAbortClarifyModalPromise()`** (reject → **`AbortError`**). Draft stores **`clarifyQuestions`/`gapClarifyQuestions`** arrays for restore; legacy drafts fall back to **`planParseNumberedQuestionsFromPrompt`**. **`planWireClarifyModalOnce`** wires Submit/Cancel/backdrop/Esc.
- **Settings collapsible rows (300426):** **`wireSettingsCollapsesOnce`** also binds **`notifSettingsCollapseHead`/`notifSettingsCollapsePanel`** and **`readAloudCollapseHead`/`readAloudCollapsePanel`** (same pattern as Inference/Messaging: click head toggles **`is-open`** on panel, **`aria-expanded`** on head; **ⓘ** does not toggle). Notifications + Read aloud default **collapsed**. **`notificationStatusText()`** leaves **`#notifyStatus`** empty unless the browser lacks **`Notification`**, **`isSecureContext`** is false (short fix strings), or **`permission === 'denied'`** — no routine permission/origin line.
- **Assistant inference footer (300426):** Under each assistant bubble, **`.msg-inference-footer`** used to render only when **`formatCodexAutoInferenceLabel`** had text (OpenAI Codex **Auto** + gateway **`X-Hermes-Codex-Auto-Model`**). **`buildAssistantInferenceLabel`** now sets **`inference_label`** as **`{CHAT_BACKEND_PRESETS.label} · {effective model}`** for all backends; **`addBubble`** falls back to **`inferenceFooterFromStoredMeta`** when older JSON has **`inference_provider` / `inference_model`** but no label.
- **Settings → Inference → Codex Sign in (300426):** Status lived only in **`#inferenceMatrixBanner`** below the long provider list (easy to miss). Per-row **`.inf-codex-status`** + **`wireInferenceMatrixRootDelegationOnce`** (delegated click on **`#inferenceMatrixRoot`**) + **`handleSettingsCodexSignIn`** with **`try`/`finally`** avoids stuck **`disabled`** and survives partial renders. Delegated handlers must resolve **`event.target`** when it is a **`TEXT_NODE`** (use **`parentElement`**) before **`closest()`**, or clicks on the label text no-op. OAuth still runs **`hermes auth add openai-codex`** on the **gateway host** (terminal); the UI polls **`/api/setup/codex-auth-status/{poll_id}`**.
- **Model id vs matrix (300426):** **`effectiveChatModelId()`** used to prefer **`localStorage`** **`animus_selected_model`** over **`settings.chat_model`**, so stale ids (e.g. preset **gpt-5.2-codex**) could override **`client-config` `inference_models`** / the Settings matrix (e.g. **gpt-5.3-codex**) and the assistant footer. Order is now **`inference_models[chat_backend]` → `chat_model` → localStorage → preset**. Toggling **Active** and **`persistChatModelPicker`** also write **`inference_models[pid]`** + **`localStorage`** together.
- **Codex OAuth — use Hermes implementation (300426):** OpenAI Codex device login lives in **`hermes_cli/codex_device_oauth.py`** (extracted from **`hermes_cli/web_server`**). ANIMUS **`/api/setup/codex-auth-start`** / **`codex-auth-status/{id}`** call that module — **no** `hermes auth add` subprocess and **no** `import web_server` in ANIMUS (avoids FastAPI/uvicorn in **`animus-chat/.venv`**). Dashboard **`/api/providers/oauth/openai-codex/start`** and poll/cancel delegate to the same module. PWA **`runCodexAuthWithPoll`** opens **`verification_url`** and shows **`user_code`**.
- **Settings ↔ Hermes global model (300426):** **`GET /api/setup/provider-status`** adds **`hermes_active_provider`**, **`hermes_default_model`**, **`hermes_active_animus_id`** (maps **`gemini`→`google`**, **`togetherai`→`together`**). **`POST /api/setup/sync-hermes-model`** body **`{ provider, model }`** (ANIMUS ids) calls **`hermes_cli.auth._update_config_for_provider`** + **`_save_model_choice`** after key/Codex checks; base URL from **`resolve_provider_full`** / **`PROVIDER_REGISTRY`** / OpenAI default. PWA **`syncHermesModelIfNeeded`** runs when the row is **`settings.chat_backend`** or matches Hermes global so the gateway CLI config stays aligned.
- **Hermes sync — CLI / ACP rows (300426):** **`cursor-agent`**, **`claude-code`**, **`copilot-acp`** sync like other backends: **`claude-code`** maps to Hermes **`anthropic`** (Claude Code OAuth path). Preconditions: Cursor authenticated (**`cursor whoami`**), Claude Code credentials (**`_claude_code_authenticated`**), Copilot CLI on PATH (**`copilot`** or **`COPILOT_CLI_PATH`** / **`HERMES_COPILOT_ACP_COMMAND`**). Unresolved base URL → **422** (hint: set **`*_BASE_URL`**). Extra fallbacks: **`mistral`**, **`groq`**, **`togetherai`**, **`cohere`**.

## Hermes gateway proxy auth (300426)

- **`HERMES_API_KEY`:** Hermes `api_server` **allows all requests when the gateway has no API key configured** (`_check_auth` returns OK). ANIMUS used to **block** `/api/chat` (and Help ask, `GET /api/models?gateway=1`) when `HERMES_API_KEY` was unset, which broke fresh installs after wizard onboarding (wizard writes provider keys, not necessarily this env var). **Fix:** `hermes_runner.gateway_upstream_headers()` omits `Authorization` when empty; `gateway_api_bearer()` reads `os.environ` each call so `save-config` / edited `animus.env` can apply without restarting. If the gateway **does** require a key, set `HERMES_API_KEY` in `animus.env` to match.
- **Gateway 401 "Invalid API key" in chat:** Hermes **`API_SERVER_KEY`** (often in **`~/.hermes/.env`**) must match what ANIMUS sends as **`Authorization: Bearer`**. v1.0.6+: **`hermes_runner.gateway_api_bearer()`** falls back to **`API_SERVER_KEY`** from **`~/.hermes/.env`** when **`HERMES_API_KEY`** is blank (mtime-cached); **`installer/merge-hermes-gateway-auth.py`** copies the same into **`animus.env`** during **`install.sh`** / **`preflight.sh`**. Diagnose: **`curl -sS http://127.0.0.1:3001/api/version | jq .gateway_bearer_source,.gateway_openai_models_ok,.gateway_openai_models_http`**. Restart: **`systemctl --user restart animus.service`** (not **`systemctl restart animus`**).
- **Stale `server.py` / no `installer/`:** If **`~/animus-fresh-install`** only has **`animus-chat/`** (no **`installer/sync-animus-chat-from-zip.sh`**), **`sync-from-release-zip.sh` is only inside the release zip** — a hand-copied folder does not have it until **`unzip -p "$ZIP" animus-chat/sync-from-release-zip.sh > sync-from-release-zip.sh`**. Then **`chmod +x sync-from-release-zip.sh && ./sync-from-release-zip.sh "$ZIP"`** from **`animus-chat/`**. Full-tree installs use **`installer/`** wrapper. **`./build-release.sh`** fails if the obsolete **`HERMES_API_KEY is not configured…`** gate string returns in **`server.py`**. Verify: **`curl -sS http://127.0.0.1:3001/api/version | jq .chat_server_rev,.chat_proxy_blocks_on_missing_hermes_api_key`**.

## Token tracker / chat SSE (300426)

- **Tail line dropped:** The chat `fetch` + `ReadableStream` loop used `buf.split('\n'); buf = lines.pop()`, so the **last** SSE `data:` line was only parsed if the stream ended with a newline after that line. The gateway’s final chunk (often the only one carrying `usage`) can arrive as the last bytes **without** a following `\n`, so **`streamUsage` stayed null**, messages had no `usage`, and the Tokens tab looked empty even when the proxy logged correctly. **Fix:** `TextDecoder` flush (`decode(Uint8Array, { stream: false })`) then iterate **all** remaining `buf.split('\n')` segments; accept `data:` with or without a space after the colon (`trimStart` + `startsWith('data:')`).
- **Server `_sse_last_usage_and_model`:** Normalize `\r\n`, read `usage` from **`choices[0]`** when top-level is absent, and map **`input_tokens`/`output_tokens`** (and **`total_tokens` only** as input, output 0) before `record_token_usage`.
- **Explicit zero usage (300426):** Hermes finish chunks include `usage: { prompt_tokens: 0, completion_tokens: 0, total_tokens: 0 }` when the model reports no billable usage (short replies like “Ready.”). **`normalizeUsagePayload`** must not drop those (treat as `{0,0,0}` when usage keys are present); otherwise `streamUsage` stays `null`, `msg.usage` is missing, and the Tokens tab chart stays empty even though **`token_usage.jsonl`** has rows. The Tokens UI should still list **recent server rows** when totals are all zero so operators see activity.
- **Gateway `_run_agent` usage dict:** Prefer **`run_conversation` result** `prompt_tokens` / `completion_tokens` / `total_tokens` for the SSE finish chunk (see `hermes-agent/gateway/platforms/api_server.py`) so streamed totals match the turn result object.
- **Codex streaming usage (300426):** `responses.stream()` + `get_final_response()` often returns **`usage=None`** even when terminal events include counts. **`run_agent._run_codex_stream`** now copies **`usage`** from **`response.completed` / `response.incomplete` / `response.failed`** events onto the final response (`_codex_merge_stream_usage`) so `session_*` counters and ANIMUS **`token_usage.jsonl`** get non-zero values when the API reports them.

## Conventions

- **Session start:** Read `AGENTS.md` then `project_goal.md`, `project_status.md`, `project_history.md`, this file, and `repo_map.md` before code edits.
- **Session end:** Append `project_history.md`; update this file with new lessons or a no-new-knowledge note; refresh `project_status.md` / `repo_map.md` when state or layout changes.

---

## Hermes CLI `hermes update` (300426)

- If update **resets to `origin/main`** and **stash restore hits conflicts**, the updater typically leaves a **clean tracked tree** (upstream only). **ANIMUS customisations that lived in conflicted files are not on disk** until you **`git stash apply <ref>`** (or cherry-pick) and **resolve** `cron/jobs.py`, `cron/scheduler.py`, `gateway/platforms/api_server.py`, `tests/cron/test_scheduler.py`, `tools/delegate_tool.py`, etc. Untracked files from before may still exist; re-verify cron/deliver/gateway behaviour after any update.
- **Source of truth:** Re-sync **`hermes-agent/`** from the **ANIMUS monorepo** or a **release zip** (`installer/sync-animus-chat-from-zip.sh` / `docs/hermes-agent-patches.md`) rather than assuming `hermes update` preserved seller patches.
- **Single dev tree:** Prefer **one** ANIMUS clone (**`/home/sketch/animus`**) for chat + agent + gateway. Run **`./scripts/sync-dev-systemd.sh`** to install **`animus.service`** and **`hermes-gateway.service`** from that **`ROOT`** (no parallel **`animus-fresh-install/hermes-agent`** path required). Use a separate unzip dir only when simulating a buyer machine; do not treat it as a second “source of truth.”
- **`hermes project`:** Handlers live in **`hermes_cli/project_workspace_cmd.py`**; **`hermes_cli/main.py`** must register the **`project`** subparser and **`cmd_project`** (init / history-append / repo-map-refresh / repo-maps-refresh-all / show / write) or CLI help will not list the command.
- **Gateway restart vs ANIMUS:** If **`hermes-gateway.service`** uses **`WorkingDirectory=/home/sketch/.hermes/hermes-agent`** (or any non-ANIMUS tree), restarting it does **not** load the monorepo **`hermes-agent/`**. Either run **`./scripts/sync-dev-systemd.sh`** so the unit tracks **`$ROOT/hermes-agent`**, or spot-check delivery with a throwaway **`CHAT_DATA_DIR`** and **`PYTHONPATH=<animus>/hermes-agent`** (no systemd) before touching a shared gateway.
- **Dev host gateway unit (300426):** **`scripts/sync-dev-systemd.sh`** writes **`~/.config/systemd/user/hermes-gateway.service`** with **`WorkingDirectory`** = **`$ROOT/hermes-agent`**, **`CHAT_DATA_DIR=$ROOT/animus-chat/data`**, **`HERMES_HOME=$HOME/.hermes`**, and **`ExecStart ... --profile default`**. Re-run after moving the repo. Backup old units before profile/chat-dir experiments (e.g. **`hermes-gateway.service.bak-20260430-dpc`**).

## Stack and validation

- **Python venv:** `cd animus-chat && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt` then `.venv/bin/python -c "import server"`.
- **Release:** `./build-release.sh` (requires `rg`; scans `animus-chat`, `hermes-agent`, `installer`, `docker`, `systemd` only — root `project_goal.md` may still mention example paths). Phase 2 greps **`animus-chat/app/index.html`** (not `static/index.html`) for legacy API paths and checks `server.py` subprocess lines are tagged `# exempt:`.
- **Default port:** `CHAT_PORT` / `PORT` default **3001** in `server.py`; live Hermes on 3000 stays untouched.

---

## 290426 — Initial repo

- Repository was empty before continuity docs; there is no `package.json` / `Cargo.toml` / etc. until added.

## Bootstrap (`setup_repo.md`)

- **`setup_repo.md`** holds full steps and Appendix A (complete `AGENTS.md` body with `[repo]` placeholder) so another repo can mirror this structure. Policy edits should stay in sync across `AGENTS.md`, `.cursorrules`, `CLAUDE.md`, and `.cursor/rules/operating_rules.md` (use `cmp` to verify).

## Desktop launcher (290426)

- **`installer/create-desktop-launcher.sh`:** Linux GUI → `~/.local/share/applications/animus.desktop` + Desktop copy (`xdg-open` URL from `HERMES_CHAT_PUBLIC_URL` or `http://CHAT_HOST:CHAT_PORT/`). macOS → `~/Desktop/ANIMUS.webloc`. Skips Docker (`/.dockerenv`), `CI`/`GITHUB_ACTIONS`, `SKIP_ANIMUS_DESKTOP_LAUNCHER=1`, non-Linux/Darwin, Linux without `DISPLAY`/`WAYLAND_DISPLAY`.
- **Wizard:** `wizPhoneLike()` gates the optional download; `localStorage.animus_desktop_launcher_offered_v1` prevents repeats.
- **API:** `GET /api/animus/desktop-launcher` uses `HERMES_CHAT_PUBLIC_URL` when set, else `X-Forwarded-*` / `Host` / `127.0.0.1:CHAT_PORT`.

## Token and navigation (290426)

- **`repo_map.md` first:** Small tree here—open listed paths only; when code lands, keep the map current so agents avoid whole-repo walks.
- **`project_history.md`:** Append new entries at EOF; do not load the full log just to add a line. Skim recent entries for context.
- **History append safety:** for large logs, take a short tail read first and patch against the current EOF anchor; do not blind-append without checking the latest tail block.
- **Mirrors:** After editing `AGENTS.md`, run `cmp` copies: `cp AGENTS.md .cursorrules CLAUDE.md .cursor/rules/operating_rules.md` (adjust paths); one source of truth.
- **`setup_repo.md`:** Appendix A must stay aligned with `AGENTS.md` (same policy body; `[repo]` vs real name is the only intentional difference).

---

## 290426 — ANIMUS bootstrap

- **`hermes skills` not `hermes skill`:** route modules call `hermes skills …` per current CLI.
- **`build-release.sh` password grep:** use word boundary (`\b5206\b`) so `uv.lock` numeric hashes do not false-positive.
- **Skills enable/disable (Option C):** `POST /api/skills/enable|disable/{id}` returns **200** with `{ ok: false, error: "…hermes skills config…" }`; UI hides toggles. Do not reintroduce 501 for these routes.

## 290426 — Phase 2 UI / wizard

- **Primary UI file:** `animus-chat/app/index.html` (single-file PWA). Wizard + tooltips live here; acceptance greps target this path.
- **Cron manual run:** UI uses `POST /api/cron/run/{id}` (alias of trigger). List uses `GET /api/cron/list`.

## 300426 — Sidebar tabs + updates + Settings (`index.html` + `server.py`)

- **Tabs:** Sidebar uses **icon-only** stabs (`stab stab--icon`) with `title` + `aria-label`: Chats, Skills, Cron, Plan, **Tokens** (`#tab-tokens`), Settings, **Help** (`#tab-help`). Token usage UI and Help center **live in tab panels** (no `#tokenUsageBackdrop` / `#helpGuideBackdrop`). Tab switch: **tokens** → `setTokenUsageTab` + `renderTokenUsageModal`; **help** → `wireHelpCenterTabsOnce` + `loadHelpGuideContent` (cached via `helpGuideContent.dataset.guideLoaded`); leaving help → `resetHelpGuideTabState`.
- **Check updates:** `POST /api/animus/check-updates` uses manifest URL **`ANIMUS_UPDATE_URL`** or, if unset, **`https://animusai.vercel.app/api/latest.json`** (`_animus_update_manifest_url()` in `server.py`); same for **`apply-update`**. UI uses **`alert` / `confirm`** (no `#checkAnimusUpdatesStatus`).
- **Inference matrix:** Per-provider **Active** is a **checkbox** (`.inf-active-cb`) styled as `ssh-toggle-row`, not `<input name="infActive" type="radio">`.

## 290426 — HELP bot (`help_routes.py` + `docs/animus-user-guide.md`)

- **`GET /api/help/guide`** — returns **`topics_markdown`** (guide with FAQ block removed), **`faq_markdown`** (body under `## Frequently asked questions`), **`topics`** `[{id,title}]` (slug ids aligned to `##` order in `topics_markdown`), plus **`markdown`** / **`full_markdown`** for backward compatibility. Help center UI: Home, Topics (nav + article), FAQ, Ask tabs.
- **`POST /api/help/ask`** — body: `question`, `model`, `hermes_provider`, optional `hermes_base_url`; non-streaming gateway completion with strict system prompt + full guide in user message; tokens logged with **`source: "help"`**. OpenAI Codex **`auto`** is rewritten to **`gpt-5.2-codex`** for this path only.
- **UI:** Help lives in **`#tab-help`** / **`#helpGuideRoot`**; **Ask ANIMUS** uses `effectiveChatModelId()` + `chatBackendPayload()` (same active model as chat).

## 290426 — Project notification unread (`index.html`)

- Per-thread read watermarks: **`hermes_notif_conv_read_counts_v1`** (`convId` → last-read message count). Replaces legacy project-aggregate **`hermes_notif_read_msg_counts_v1`** badge math.
- Unread notification rows: class **`notif-item-unread`** (accent background, white text). Opening a thread or expanding the Notifications section marks read.

## 290426 — Settings SSH host modal (`index.html`)

- **Auth radios:** `.field input{width:100%}` stretches `<input type="radio">`. Fix with a wrapper id (e.g. `#sshFormAuthRadios`) and scoped overrides like `#ttsBackendRadios` (width auto, label `display:flex`, option text in `<span>`).
- **Toggles:** Keep the same checkbox ids (`sshFormIdentitiesOnly`, `sshFormStrictOff`) for save/test JSON; style as switches with a row `<label class="ssh-toggle-row">` so the label text is clickable. Place toggle blocks outside a `.field` wrapper if checkboxes would inherit `.field input{width:100%}`.

## 290426 — Phase 3 alignment

- **`chat_data_dir()`** in `hermes_runner.py` matches `server.DATA_DIR` (CHAT_DATA_DIR / ~/.hermes/.env / default `~/.hermes/chat`). Wizard `config.json` + model cache use it — avoids wizard living in `./data` while chats live under `~/.hermes/chat`.
- **Wake lock:** persisted via `GET/POST /api/animus/client-config` and wizard `save-config` `wake_lock`; client loads on init after wizard.
- **Skills toggles:** `GET /api/skills/capabilities` drives `#skills-container[data-enable-toggle]`; CSS hides `.skill-enable-toggle` when false.
- **Smoke doc:** `scripts/phase3-smoke-checklist.md` — run on a host with Docker + gateway; update `project_status.md` table with results.
- **Smoke setup:** there is often **no** `animus-chat/data/`; wizard + chats use **`chat_data_dir()`** (`CHAT_DATA_DIR` → `~/.hermes/.env` → `~/.hermes/chat`). Use `CHAT_DATA_DIR=/tmp/...` for a clean wizard without touching your real `~/.hermes/chat`.
- **Support / “where did my data go?”** Ask which resolution step applies: `CHAT_DATA_DIR` env, `HERMES_CHAT_DATA_DIR`, keys in `~/.hermes/.env`, or default `~/.hermes/chat`. **`CHAT_DATA_DIR`** is the clean escape hatch for smoke tests and for users who want a dedicated folder.
- **Docs / examples:** Prefer **`python3`** or **`animus-chat/.venv/bin/python`** in shell snippets — `python` alone is often missing on Debian/Ubuntu.

## Hermes Agent bundle vs upstream

- **Smoke Step 2 (host):** bundled **v0.11.0 (2026.4.23)**, Python 3.11.15; upstream was **778 commits ahead** at that check — not a v1.0 blocker (shipped snapshot is intentional).
- **Operators:** Do **not** run **`hermes update`** inside ANIMUS expecting to keep patch alignment; see top of **`docs/hermes-agent-patches.md`**.
- **v1.1+:** Rebasing ANIMUS patches onto newer upstream Hermes Agent is a scheduled merge/re-test effort; the patch audit doc is the merge checklist.

## 290426 — Phase 7 (Slack/SSH, token audit, Claude Code, PWA paths)

- **`build-release.sh`:** Phase 7 greps — `SLACK_WEBHOOK_URL` in `animus.env.example`, `api/ssh/hosts` + `"source"` strings in `server.py` (module docstring), no `Browse` / “not available in the PWA” in `app/index.html`.
- **Token usage:** `animus-chat/token_usage.py` + `GET /api/tokens/usage?days=&source=`; chat route appends on SSE completion; cron `trigger` logs `source=cron`. UI merges local conv `usage` with server JSONL.
- **Slack / SSH:** `integrations_slack.py` and `ssh_routes.py` mounted from `server.py`; Settings panels open modals; Slack save merges keys into repo-root `animus.env` (preserve other lines).
- **Claude Code:** UI `claude-code` preset + matrix row; catalog from `_claude_code_catalog_rows()` (Hermes `_PROVIDER_MODELS["anthropic"]` + `auto`). Wizard `provider-status` probes `agent.anthropic_adapter` for Claude Code OAuth.
- **Remote projects:** local path auto `/<projects_sync_root>/_ssh_mounts/<alias><remote_abs_path>`; user must still `sshfs` if they want `fs/validate` to pass before mount exists.

## 290426 — Plan tab + manual token usage

- **`POST /api/tokens/record`** (`token_usage.py`): append-only JSONL; body uses **`provider`**, **`model`**, **`input_tokens`**, **`output_tokens`**, **`source`**, **`source_id`**.
- **Plan:** `planHermesChat` parses non-stream chat JSON and returns **`usage`** when present; **`planRunStage`** calls **`recordTokenUsageManualIfPresent`** with **`source: plan`** and **`source_id`** = pipeline stage id. Frontend skips the POST when **`usage`** lacks finite **`prompt_tokens`** / **`completion_tokens`** (avoids null-only client rows).
- **Skills UI:** same helper after successful install/create/update/toggle/wizard **update-all** when response JSON includes **`usage`** (currently unused until an API adds it).

## Settings form CSS gotcha

- **`.field input { width:100% }`** applies to **radio** inputs unless overridden (see **`.chat-backend-field`** and **`#ttsBackendRadios input[type="radio"]`**). Without that, radios stretch full width and label text collapses to a narrow column.

## 290426 — Splash / startup image

- First-paint splash in **`animus-chat/app/index.html`** (`#splash-screen`) uses **`/ANIMUSLOGO.png`** from **`animus-chat/app/ANIMUSLOGO.png`** (keep in sync with repo-root **`ANIMUSLOGO.png`** when the brand asset changes).
- Setup wizard step 0 (Welcome) uses **`/ANIMUSLOGOICON.png`** from **`animus-chat/app/ANIMUSLOGOICON.png`** (sync from repo-root **`ANIMUSLOGOICON.png`** when the icon changes).

## Hermes upstream drift vs ANIMUS

- **`hermes --version`** may print “commits behind — run `hermes update`”. That is Hermes CLI behaviour, not ANIMUS auto-updating the tree.
- **Do not** add a server or UI feature that periodically runs **`hermes update`** against the ANIMUS-bundled `hermes-agent/` without a controlled rebase + full acceptance test — **`docs/hermes-agent-patches.md`** (banner) forbids it; it can remove Cursor/skills/cron/chat alignment patches.
- **Wizard step 1** is titled **Agent check**: only shows reachable vs failed (no full `hermes --version` dump in the UI); `/api/setup/hermes-check` still returns `version` for debugging if needed later.

## 290426 — Phase 6 (models, cron, skills create, Piper TTS)

- **`POST /api/models/refresh`:** Rebuilds local `hermes_models_cache.json` from bundled Hermes `_PROVIDER_MODELS` + **`list_cursor_cli_models()`** when `cursor whoami` succeeds — **no `HERMES_API_KEY`**. `GET /api/models` without `?gateway=1` serves this list for the UI.
- **Cron UI:** `populateCronProjectDropdown()` → `GET /api/projects/list-simple`; timezone suggestions → **`GET /api/system/timezones`** (async `fillCronTimezoneDatalist`); saving a job persists **`cron_timezone`** via `POST /api/animus/client-config`.
- **Piper:** `animus-chat/tts_routes.py` — optional `piper` on PATH or **`PIPER_BIN`**; voices from **`PIPER_VOICES_DIR`** or `~/.local/share/piper`, `~/.piper/voices`, etc. Operator doc: **`docs/tts.md`**.
- **Piper default voices:** **`tts_routes.py`** auto-downloads six models (~380 MB), **first** `en_GB-alan-medium` (product default for Read aloud + Settings), same order as **`installer/fetch-piper-voices.sh`**, when **`piper`** is on PATH/`PIPER_BIN` but no `.onnx` files exist. API **`fetching`** + UI poll; **`SKIP_ANIMUS_PIPER_VOICES=1`** disables.
- **TTS defaults:** `GET /api/animus/client-config` uses **`tts_backend` `piper`** when the key is absent (`server.py`); client **`DEFAULT_PIPER_VOICE_ID`** in `app/index.html` matches **`en_GB-alan-medium`**. UI still falls back to browser if Piper is not installed.
- **Skills create:** `POST /api/skills/create` writes `SKILL.md` under `~/.hermes/skills/<name>/` (validated id pattern).

## 290426 — Phase 5 (wizard + settings + updates)

- **Wizard flow:** Step order is 0 Welcome → 1 **Agent check** (Hermes CLI reachable; no `hermes --version` wall in UI) → 2 provider checklist + inline auth (no standalone Cursor step) → 3 default model (uses `wizard_ready_providers` / `wizard_selected_providers` from `GET /api/animus/client-config` to filter; refresh models after Cursor when needed) → 4 skills manager (`/api/skills/list` + enable/disable when capabilities allow) → 5 projects folder (`GET /api/setup/check-path`, saves `projects_dir`) → 6 Tailscale (`GET /api/setup/tailscale-check`) + wake lock only when Tailscale enabled → 7 Done (no wake checkbox).
- **Settings inference:** `GET /api/setup/provider-status` drives the matrix; `POST /api/setup/test-key` + `save-config` keys update env; Codex: `POST /api/setup/codex-auth-start` spawns background `hermes auth add openai-codex` and returns `poll_id` — UI polls `GET /api/setup/codex-auth-status/{poll_id}` every 3s (180s client timeout); session probe is `GET /api/setup/codex-auth-session`. Cursor: `POST /api/setup/cursor-login-start` + re-check.
- **Updates (Phase 8):** `POST /api/animus/check-updates` / `apply-update` read **`ANIMUS_UPDATE_URL`**, compare manifest **`version`** to repo **`VERSION`**, download **`download_url`** zip, **`extractall`** with path validation. **`installer/install.sh`** does not touch git.
- **animus-update-server:** Run **`<that-dir>/.venv/bin/uvicorn`** (or systemd **`ExecStart`** pointing at that binary). A global **`~/.local/bin/uvicorn`** has no **fastapi** → `ModuleNotFoundError: fastapi`. **`[Errno 98]` on 9977** (default in unit): stop duplicate **`animus-updates`** / old uvicorn (see **`ss -tlnp`** / **`lsof -i :9977`**) or change **`--port`**. User unit lives in **`~/.config/systemd/user/animus-updates.service`** — **`cp`** from repo **`systemd/`** after edits. **`EnvironmentFile=-/path/.env`** makes **`.env`** optional; create **`.env`** from **`.env.example`** and set **`ADMIN_TOKEN`** for admin routes. **Tailscale:** uvicorn is **HTTP** only; TLS is at Tailscale. **`tailscale serve --https=443 /updates/ http://127.0.0.1:9977`** → buyer **`ANIMUS_UPDATE_URL`** = **`https://<machine>.ts.net/updates/latest.json`**. **`StaticFiles`:** mount **`/updates/releases`** (full path) **and** **`/releases`** (Tailscale often strips **`/updates`** on the proxied path); register mounts **after** API routes. Zips live in **`releases/zips/`**.
- **Never create a venv inside `animus-chat/`** (for agent smoke / extra envs): `build-release.sh` walks that directory and pip’s vendored files cause **sanitisation grep false positives**. Use **repo-root `.venv`** or a path **outside** `animus-chat/`. Exception: **`install.sh`** creates `animus-chat/.venv/` by design; that tree is excluded from the zip and is the supported layout for end users.
- **Release checks:** `build-release.sh` greps Plan-tab strings `Each Hermes step|Hermes step is|Hermes chat`, asserts `check-updates` in `server.py`, and `projects_dir` in `wizard_routes.py`.
- **`GET /api/version`:** includes `python` short version for About modal.

## Go-live smoke (operator)

- **Before step 1:** Open **`animus.env`** and/or **`~/.hermes/.env`** and confirm at least one **real** provider key (not placeholder / not expired). Wizard may proceed without it, but **step 3 (key test)** and **step 9 (chat)** give misleading results if the key is bad.
- **During step 9:** Watch **browser Network** (SSE) or **server stdout**. The first assistant stream chunk that includes a **`usage`** block means the **token tracker** can populate once you open it — you may see proof before opening Settings → Token tracker.
- **Screenshots (Gumroad):** Capture **during** the smoke run (wizard welcome, chat with badge, cron list) while **`CHAT_DATA_DIR`** still shows first-run UI. After teardown + return to normal data, the wizard is gone unless you reset **`config.json`** or start another throwaway **`CHAT_DATA_DIR`** session.
- **Step 16 (`./build-release.sh`):** Run **last**, from **repo root**, in a **clean shell** — **`unset CHAT_DATA_DIR`** after smoke teardown so the release build is not tied to the temp tree. The script does not need smoke data, but avoiding a stray env var avoids confusion.

## Buyer install — sshpass (300426)

- **`POST /api/ssh/test`** with password auth shells out to **`sshpass`** (`animus-chat/ssh_routes.py`). **`installer/ensure-sshpass.sh`**: interactive apt/dnf/pacman/zypper (Linux) or **`brew install sshpass`** (macOS); **`SKIP_ANIMUS_SSHPASS=1`** skips; non-tty **`install.sh`** prints one-line manual commands. **`docker/Dockerfile`** installs **`sshpass`** in the image.
- **SSH password probe:** OpenSSH **`BatchMode=yes`** disables password authentication — **`sshpass`** alone is not enough. Password path must use **`BatchMode=no`** plus **`PubkeyAuthentication=no`** and **`PreferredAuthentications=password,keyboard-interactive`** so the client does not burn **`MaxAuthTries`** on local keys first.

## Cron tab — project scope, overseer, deliver, optimize (300426)

- **Project scope** maps to gateway job **`workdir`** (absolute project **`path`**); Hermes **`create_job`** rejects missing/non-dir paths — the project folder must exist on the gateway host.
- **Stored prompt** = optional **`cron_overseer_prompt`** (client-config + local **`settings`**) + fixed invariant (project name + `project_history.md` / `repo_map.md` / token + notification guidance) + user task, separated by **`--- ANIMUS cron task ---`**; the modal textarea edits **only the task**; re-saving refreshes the prefix from current overseer/settings.
- **Deliver dropdown:** built from **`GET /api/messaging/overview`**; each row may include **`cron_deliver_home_ready`** (enabled + connected + non-empty gateway **home channel**). Bare **`deliver=telegram`** (etc.) uses Hermes cron home resolution — same as comma-separated multi-target support in **`cron/scheduler.py`**.
- **Prompt optimizer:** **`POST /api/cron/optimize-prompt`** — non-stream **`/v1/chat/completions`**; body may include **`model`** / **`hermes_provider`** from the user’s chat settings (defaults **`gpt-4o-mini`** + **`openai`** on the server).
- **Quick validate:** `python3 -m py_compile animus-chat/cron_routes.py animus-chat/server.py animus-chat/messaging_routes.py`
- **Cron timezone “Change”:** A text field + **`<datalist>`** does not open a picker from JS (`focus`/`select` only highlights). **Change** opens **`<dialog id="cronTzDialog">`** with a filter box + large **`<select>`** of IANA zones (from **`window.__cronTzZones`** filled by **`fillCronTimezoneDatalist`**); **Apply** copies the choice into **`cronTzInput`**. Browsers without **`HTMLDialogElement.showModal`** fall back to **`window.prompt`**.

## Chat — "No reply text was received" (010526)

- **Client behavior (`animus-chat/app/index.html`):** After **`POST /api/chat`** SSE ends, if **`streamText`** is still empty and no top-level **`error`** object was parsed from any `data:` line, the UI injects the stock **"No reply text was received…"** hint (inference backend + **`HERMES_API_URL`** reachability).
- **Registry gap (fixed Patch 11):** **`hermes_provider: cursor-agent`** failed because **`hermes_cli/providers.py`** had no **`HERMES_OVERLAYS["cursor-agent"]`** — **`resolve_provider_full`** returned **None** while **`run_agent` / `cursor_agent_client`** already supported Cursor. **Fix:** register **`cursor-agent`** in **`HERMES_OVERLAYS`** (+ aliases **`cursor`**, **`cursor-cli`**) so the gateway resolves the provider; then **`systemctl --user restart hermes-gateway.service`**.
- **Regression guard:** **`hermes-agent/tests/hermes_cli/test_provider_registry_external_shims.py`** — for every **`hermes_cli.auth.PROVIDER_REGISTRY`** entry with **`auth_type == "external_process"`**, assert **`HERMES_OVERLAYS`** contains the id and **`resolve_provider_full`** succeeds. Add a matching **`HERMES_OVERLAYS`** row whenever a new subprocess inference provider is added to **`PROVIDER_REGISTRY`**.
- **After provider resolves:** Empty replies can still mean missing **`cursor-agent`** on **`PATH`**, **`CURSOR_API_KEY`**, or logged-in **`cursor`** CLI — see gateway logs and **`hermes doctor`**.
- **Quick checks:** (1) **`python3 -c "import os; os.environ.setdefault('PYTHONPATH','hermes-agent'); from hermes_cli.providers import resolve_provider_full; print(resolve_provider_full('cursor-agent',{},None))"`** from repo root (with **`PYTHONPATH=hermes-agent`**) should return a **`ProviderDef`**, not **`None`**. (2) **`curl -sS http://127.0.0.1:3001/api/version | jq .gateway_openai_models_ok,.gateway_bearer_source`**. (3) Non-stream probe with the same JSON body the UI sends — surfaces auth / CLI errors once provider resolution works.

## No-new-knowledge template

When a session adds nothing durable, append:

```text
DDMMYY — No new durable knowledge (routine doc-only / trivial change).
```

290426 — No new durable knowledge (animus-site copy cleanup only).
290426 — No new durable knowledge (animus-site visual brand/layout tweak only).
290426 — No new durable knowledge (animus-site Gumroad URL swap only).
300426 — No new durable knowledge (VERSION 1.0.7 + `./build-release.sh` zip only).
