@AGENTS.md

# Claude adapter

`AGENTS.md` is the canonical contract. Follow its read order, bounded reads, write rules, and **Final Response Must Include** checklist.

## Pointer

- **Cursor priority and read policy:** `.cursor/rules/00-project-contract.mdc`

Do not duplicate long policy from `AGENTS.md` here; extend only with Claude-specific execution notes when needed.
