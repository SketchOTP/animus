# Tailscale Serve

This file is kept for backwards compatibility. **Authoritative guidance** for ANIMUS is in the monorepo:

`../docs/tailscale.md`

Summary: bind ANIMUS on loopback (`CHAT_HOST=127.0.0.1`, default `CHAT_PORT=3001`) and use `tailscale serve` to expose HTTPS on your tailnet.
