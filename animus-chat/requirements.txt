httpx>=0.27
uvicorn[standard]>=0.30
starlette>=0.37
# Required for multipart form parsing (POST /api/stt/transcribe, /api/attachment/text, etc.).
python-multipart>=0.0.9
python-dotenv>=1.0
# Local STT for POST /api/stt/transcribe when HERMES_CHAT_STT_LOCAL_EMBEDDED=1 (ships in buyer zip; install.sh).
# Pins upper bound for reproducible upgrades; ctranslate2 is pulled in as a dependency.
faster-whisper>=1.0.0,<2
